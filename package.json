{
  "name": "arango-server",
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "tsc && node --eval \"import('fs').then(fs => fs.chmodSync('build/index.js', '755'))\"",
    "watch": "tsc --watch && node --eval \"import('fs').then(fs => fs.chmodSync('build/index.js', '755'))\""
  },
  "dependencies": {
    "@modelcontextprotocol/create-server": "^0.12.0",
    "arangojs": "^9.2.0",
    "typescript": "^5.7.3",
    "@types/node": "^20.17.14"
  }
}
